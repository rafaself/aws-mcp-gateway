# AWS credentials (configure with wrangler secret put in production)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=

# Authentication token for the MCP endpoint
MCP_AUTH_TOKEN=

# Optional settings
# AWS_ALLOWED_REGIONS=sa-east-1,us-east-1
# APP_ENV=production
# MCP_NAME=aws-mcp-gateway
